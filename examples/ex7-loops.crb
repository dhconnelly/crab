fn fact(n) {
  let x = 1;
  loop {
    if (n < 1) {
      break;
    }
    x = x * n;
    n = n - 1;
  }
  return x;
}

print(fact(-1));
print(fact(0));
print(fact(1));
print(fact(2));
print(fact(3));
print(fact(4));
print(fact(10));
