#[derive(Debug)]
pub enum Instr {
    GetStack(usize),
    SetStack(usize),
    PopStack(usize),
    PushInt(i32),
    PushBool(bool),
    PushStr(String),
    GetGlobal(usize),
    SetGlobal(usize),
    Negate,
    Add,
    Sub,
    Mul,
    Div,
    Eq,
    Less,
    Print,
    Jump(usize),
    JumpIfNot(usize),
}
